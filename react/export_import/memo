1. can export any variables and functions
2. export default

two ways:
export default function(num1, num2) {
	return num1 + num2
}

------

function sum(num1, num2) {
	return num1 + num2
}

export default sum;


3. import

to import default
import sum from  "./example.js"


no default, need { }
import {sum} from "./example.js"

4. can mix default and no default in one module

export let color = "red"
export default function(num1,num2) {
	return num1+num2;
}

-----
import sum, {color} from "./example.js"
